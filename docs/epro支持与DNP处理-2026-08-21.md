# .epro 支持修复、案例实测与 DNP 链路语义 — 2026-08-21

用户提出三件事：① .epro 是否支持直接选中；② 用两个真实 .epro 案例
（`D:\WorkDesigns\3_WorkTools\sch_review_tool\examples`）检查实际使用问题；
③ 不上BOM/不上PCB（DNP）标志是否纳入链路分析——0R 标了不上BOM 应视为
NC（两脚不通），而不是直接合并为同一网络。

## 一、结论

1. **.epro 此前从未真正跑通**（一进 list 就崩），本次修复后**直接支持**：
   `--eprj x.epro` 显式指定，或目录内只有 .epro 时 `find_eprj` 自动选中。
2. 两案例实测暴露 4 个问题，全部修复（见下）。
3. **DNP 已纳入链路分析**：0Ω/短接符标 `Add into BOM=no` 或
   `Convert to PCB=no` 时两脚**不再合并**，输出带 `dnp:true`/`[DNP]` 标记。

## 二、.epro 后端修复（此前不可用的根因）

| # | 问题 | 根因 | 修复 |
| --- | --- | --- | --- |
| P1 | `list` 即崩 `'dict' object is not callable` | `EproDB.__init__` 的数据属性 `self.schematics` **遮蔽了同名方法** `schematics()`——后端从未用真实 .epro 测过 | 数据属性改名 `_devices/_symbols/_boards/_schematics` |
| P2 | pinmap/pins/nets/netlist 崩 `KeyError: domain_of_pt` | **.epro 的 WIRE segs 是平铺点链** `[x1,y1,x2,y2,y3,...]`（相邻点成段），V2 是嵌套段 `[[x1,y1,x2,y2],...]`；旧代码只取每段前两点，折线中段端点全丢，连通域断裂 | 新增 `_norm_segs()` 归一化两种格式，4 个消费点（parse_sheet stub 端点、_collect_pinmap_data endp/pt_wires、resolve 步1+线段表、cmd_pinmap wire_pts）统一走它 |
| P3 | 元件 title 全是 uuid 串 | .epro 的 `COMPONENT a[2]` 是**符号 uuid 引用**（V2 是标题字符串）；实例名在 `Name` 属性 | EproDB.sheet_records 归一化 a[2]→空；parse_sheet 对空 title 用 `Name` 属性兜底 |
| P4 | --json 输出全部 JSON-INVALID | `[lceda_reader] 检测到 .epro...` banner 打到 stdout，污染 json 数据通道 | banner 改 stderr |

## 三、案例实测结果

- **E1 = ProPrj_Piezo_Driver（5MB）**：4 个板修订（quadPizeoDriver_RevA×3 +
  dualPizeoDriver）+ 8 个 CBB 模块页 + 废案存档，17+ 页/板。
  修复后 components/bom/netlist/datasheets/boards/devmap 及逐页
  pinmap/pins/nets 全部 rc=0 且 json 有效。
- **E2 = ProPrj_TPS56C230_Buck（340KB）**：单页 P1，原 KeyError 页修复后
  pinmap 2085 行 / pins 617 行 / nets 9 行正常。
- 同名页歧义告警在 E1 生效（4 个修订版共享页名，未指定板名时 stderr 提示）。

## 四、DNP（不上BOM/不上PCB）链路语义

### 标志来源（实例级 ATTR，两案例实测）

- `Add into BOM = yes/no`（E1 238 处、E2 7 处；E1 有 **76 个不上BOM 的 R**，
  含多个 DNP 0Ω）
- `Convert to PCB = yes/no`（E1 63 处，本案例未用 no 值）

### 语义实现

- `_is_dnp(c)`：两标志任一为 `no` 即 DNP；
- `resolve_nets_by_domain`：DNP 的 0Ω 跳线/短接符**不合并两脚域**（物理未
  贴装）；非桥器件本就不合并，行为不变；
- 输出标记：`pinmap --json` 元件行加 `"dnp": true`，非 json 行头加 `[DNP]`；
  bridges 输出加 `"dnp"` 字段且 `direct` 恒为 false。

### 实测（E1 DAC_AFE_A 页）

| 器件 | 标志 | 两脚网络 | 判定 |
| --- | --- | --- | --- |
| R235（0Ω） | Add into BOM=no | `1=DAC1_OUT_BUFF_A`，`2=''` | ✅ 不合并 + dnp:true |
| R136（0Ω） | （正常） | `1=2=DAC0_OUT_A` | ✅ 照常合并 |

bridges：该页 22 个两脚器件中 4 个 DNP（R235/R238 jumper、R5/R187 passive），
`direct` 全部 false。

## 五、回归验证

| 项 | 结果 |
| --- | --- |
| smoke2（LcedaDB 9 项） | ALL PASS |
| netlist / pins / nets（涡流新旧工程） | 与基线逐字节一致 |
| pinmap | 仅新增 `dnp:false` 字段与 NetFlag symbol 列填充（预期），几何/网络零变化 |
| bom | 与昨日修复后基线一致（diff=0） |
| E1/E2 全命令 | rc=0，json 有效，KeyError 消除 |
| DNP 0Ω | 不合并 + 标记正确（上表） |

## 六、修复优雅性复查（二次审查）

对前述修复逐项排查"回避问题"式处理：

**确认为回避并补修（1 项）**：
- `resolve_nets_by_domain` 的 `des2dnp` 只映射真实 Designator——短接符/
  NetFlag **无 Designator 属性**，合成位号 `SHORT{cid}` 查不到映射，
  DNP 短接符会被错误合并。已修：同时映射 `SHORT{cid}/PORT{cid}`。
  （两案例暂无 DNP 短接符实例——E1 119 个短接符全非 DNP、涡流 3 个亦然，
  属防御性修复，单元验证通过。）

**确认为正解非回避（6 项）**：属性遮蔽改名、`_norm_segs` 统一归一化
（4 消费点同源）、banner 入 stderr、0Ω 精确 token、DNP 不合并、
trace BFS 排序确定性。

**可辩护的取舍（如实记录）**：
1. **EproDB COMPONENT a[2] 归一化为空串**丢弃了符号 uuid 引用——实测
   Symbol 属性 **100% 覆盖**（E1 3575/3575 实例），信息完全冗余无损失；
   若未来出现无 Symbol 属性的 .epro 需补兜底（记录在案）。
2. **title←Name 兜底**使 LcedaDB NetFlag 的 symbol 列从空变为端口名
   （如 `+3.3V`）——增量改进，几何/网络零变化（netlist/pins 逐字节一致）。
3. **on_segment 共线容差 0.75**：有界启发式，仅服务"引脚压线段中间"的
   T 型场景（实测当前工程 0 例触发），为语义完备性保留。
4. **conn_nets 前缀+desc 关键词**：连接器在格式内无显式类型字段，启发式
   不可避免；已排除合成位号、覆盖 desc 关键词，且定位是"候选+人工确认"。

## 七、E1 案例的 CBB（复用块）实测

**CBB 内部位号**（用户问询）：

| CBB 页 | 元件数 | 位号 | 主器件 |
| --- | --- | --- | --- |
| `_CBB_ADHV4702__NOINV_2L` | 37 | U1(×3 part), C1-C6, D1, D2, R1-R4, R7-R10 + 19 无位号(电源符号) | ADHV4702-1BCPZ |
| `_CBB_MAX5318_2L` | 60 | U13, C40-C47/C58/C59/C62, D1, R23-R31, TP1 | MAX5318GUG+T |
| `_CBB_LDO_TPS7A3001_2LAYER` | 14 | U1, C1-C5, R1-R3（EN/VIN/VOUT 为无位号 NetPort） | TPS7A3001 |
| `_CBB_LDO_TPS7A4901_2Layer` | 14 | **U45, R219-R221, C247-C251** | TPS7A4901DGNR |
| `_CBB_SMPS_TPS563201_BUCK_ONELAYER` | 16 | U1, L1, C1-C6, R1-R3 | TPS563201DDCR |

（`_old`/`_old_1`/`_old_2` 为历史版本 CBB，内容与现行版相同。）

**母图引用方式**：`Designator=CBB1..CBB13` 的复用块实例（symbol_type=17、
18 引脚），工具已正确纳入连通域（ControlDAC_A 页 comp_pins 含
`('CBB6','e14754')` 等键）。

**⚠️ 位号空间风险**：CBB 内部位号不统一——有的用**本地独立位号**
（U1/C1，如 ADHV4702/TPS7A3001/TPS563201 CBB），有的已改**母图全局位号**
（U45/R219，如 TPS7A4901 CBB）。同一板引用多个"本地位号" CBB 时，跨页
归并（netlist/trace/find）会把不同 CBB 的同名位号（两个 U1、两个 C1）
当作同一器件。这是 LCEDA 复用块机制的数据特征，工具如实呈现；
**审查 CBB 电路时必须按"页"区分位号**。

## 八、遗留

- `.epro2/.epru`（V3 增量日志备份）仍不支持（历史归档用途，优先级低）；
- E2 出现自定义属性（`测试点上BOM=否`）——非标准标志不参与 DNP 判定，
  如需可后续加自定义白名单；
- EproDB 的 CBB 页已可经 `boards`/页名访问（`_CBB_xxx::P1`），但多工程命令
  （netfind/trace/link-check/find/search）尚未在 .epro 上验证多开场景。